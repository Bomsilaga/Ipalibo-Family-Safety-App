<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter's template only declares INTERNET in the debug/ and
         profile/ manifests, so a *release* build ships without it and
         every Supabase call fails silently on a real device. It has to be
         declared here to end up in the release APK. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Biometric app lock (local_auth) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Live Location / SOS check-ins (geolocator). COARSE is declared
         alongside FINE because Android 12+ lets the user grant only
         approximate location, and the app must still work in that case. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Voice/video calls (Daily.co room in a WebView) and chat photo
         attachments. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Reminder/notification delivery (flutter_local_notifications). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- The camera is used for call video and attachments, but a device
         without one should still be able to install and use everything
         else. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="The Ipalibos"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Live Location map tile (docs/06-deviations.md). This key needs
             its own Android-restriction (package name + SHA-1) in Google
             Cloud Console — it cannot share an HTTP-referrer restriction
             with the web key of the same value; get a separate key per
             platform before shipping to app stores. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAkerJDhUS9NPLnKQEVGMSh0E55ufdlONc" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
